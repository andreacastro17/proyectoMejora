[pytest]
markers =
    gui: tests that require tkinter UI
    slow: tests that are slow to execute (can be skipped with --skip-slow)
    network: tests that hit real external services (can be skipped by default)

